{
  "name": "lichfolky.github.io",
  "version": "1.0.0",
  "description": "My github page",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  }
}